// This file is auto-generated by @hey-api/openapi-ts

import { queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { type Options, PuddleStorefrontApi } from '../sdk.gen';
import type { AccountsAuthGetSessionData, AccountsAuthGetSessionError, AccountsAuthGetSessionResponse, AccountsAuthRequestOtpData, AccountsAuthRequestOtpError, AccountsAuthRequestOtpResponse, AccountsAuthSignOutData, AccountsAuthSignOutError, AccountsAuthSignOutResponse, AccountsAuthVerifyOtpData, AccountsAuthVerifyOtpError, AccountsAuthVerifyOtpResponse, AccountsGetData, AccountsGetError, AccountsGetResponse, AccountsMarketingGetData, AccountsMarketingGetError, AccountsMarketingGetResponse, AccountsMarketingUpdateData, AccountsMarketingUpdateError, AccountsMarketingUpdateResponse, AccountsOrdersGetData, AccountsOrdersGetError, AccountsOrdersGetResponse, AccountsOrdersListData, AccountsOrdersListError, AccountsOrdersListResponse, AccountsUpdateData, AccountsUpdateError, CartAddData, CartAddError, CartAddResponse, CartCheckoutData, CartCheckoutError, CartCheckoutResponse, CartCountData, CartCountError, CartCountResponse, CartGetData, CartGetError, CartGetResponse, CartMigrateData, CartMigrateError, CartRemoveData, CartRemoveError, CartRemoveResponse, CartUpdateData, CartUpdateError, CartUpdateResponse, CollectionsGetCollectionData, CollectionsGetCollectionError, CollectionsGetCollectionProductsData, CollectionsGetCollectionProductsError, CollectionsGetCollectionProductsResponse, CollectionsGetCollectionResponse, CollectionsGetCollectionsData, CollectionsGetCollectionsError, CollectionsGetCollectionsResponse, ContentBannerTextData, ContentBannerTextError, ContentBannerTextResponse, ContentBlockData, ContentBlockError, ContentBlockResponse, ContentBlocksData, ContentBlocksError, ContentBlocksResponse, ProductsGetInfiniteProductsData, ProductsGetInfiniteProductsError, ProductsGetInfiniteProductsResponse, ProductsGetProductData, ProductsGetProductError, ProductsGetProductResponse, ProductsGetSearchProductsData, ProductsGetSearchProductsError, ProductsGetSearchProductsResponse, ProductsGetTrendingProductsData, ProductsGetTrendingProductsError, ProductsGetTrendingProductsResponse, WishlistAddData, WishlistAddError, WishlistAddResponse, WishlistClearData, WishlistClearError, WishlistClearResponse, WishlistGetData, WishlistGetError, WishlistGetResponse, WishlistRemoveData, WishlistRemoveError, WishlistRemoveResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const accountsGetQueryKey = (options: Options<AccountsGetData>) => createQueryKey('accountsGet', options);

/**
 * Get current customer
 *
 * Retrieves the currently authenticated customer session profile. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const accountsGetOptions = (options?: Options<AccountsGetData>) => queryOptions<AccountsGetResponse, AccountsGetError, AccountsGetResponse, ReturnType<typeof accountsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().accountsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsGetQueryKey(options)
});

/**
 * Update customer profile
 *
 * Updates the profile details for the authenticated customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable. Requires a valid customer session cookie for the resolved storefront.
 */
export const accountsUpdateMutation = (options?: Partial<Options<AccountsUpdateData>>): UseMutationOptions<unknown, AccountsUpdateError, Options<AccountsUpdateData>> => {
    const mutationOptions: UseMutationOptions<unknown, AccountsUpdateError, Options<AccountsUpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().accountsUpdate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const accountsMarketingGetQueryKey = (options?: Options<AccountsMarketingGetData>) => createQueryKey('accountsMarketingGet', options);

/**
 * Get marketing consent
 *
 * Retrieves the marketing consent status for the authenticated customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable. Requires a valid customer session cookie for the resolved storefront.
 */
export const accountsMarketingGetOptions = (options?: Options<AccountsMarketingGetData>) => queryOptions<AccountsMarketingGetResponse, AccountsMarketingGetError, AccountsMarketingGetResponse, ReturnType<typeof accountsMarketingGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().accountsMarketingGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsMarketingGetQueryKey(options)
});

/**
 * Update marketing consent
 *
 * Updates the marketing consent status for the authenticated customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable. Requires a valid customer session cookie for the resolved storefront.
 */
export const accountsMarketingUpdateMutation = (options?: Partial<Options<AccountsMarketingUpdateData>>): UseMutationOptions<AccountsMarketingUpdateResponse, AccountsMarketingUpdateError, Options<AccountsMarketingUpdateData>> => {
    const mutationOptions: UseMutationOptions<AccountsMarketingUpdateResponse, AccountsMarketingUpdateError, Options<AccountsMarketingUpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().accountsMarketingUpdate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const accountsOrdersListQueryKey = (options?: Options<AccountsOrdersListData>) => createQueryKey('accountsOrdersList', options);

/**
 * List customer orders
 *
 * Retrieves a list of orders for the authenticated customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable. Requires a valid customer session cookie for the resolved storefront.
 */
export const accountsOrdersListOptions = (options?: Options<AccountsOrdersListData>) => queryOptions<AccountsOrdersListResponse, AccountsOrdersListError, AccountsOrdersListResponse, ReturnType<typeof accountsOrdersListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().accountsOrdersList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsOrdersListQueryKey(options)
});

export const accountsOrdersGetQueryKey = (options: Options<AccountsOrdersGetData>) => createQueryKey('accountsOrdersGet', options);

/**
 * Get order
 *
 * Retrieves details of a specific order for the authenticated customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable. Requires a valid customer session cookie for the resolved storefront.
 */
export const accountsOrdersGetOptions = (options?: Options<AccountsOrdersGetData>) => queryOptions<AccountsOrdersGetResponse, AccountsOrdersGetError, AccountsOrdersGetResponse, ReturnType<typeof accountsOrdersGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().accountsOrdersGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsOrdersGetQueryKey(options)
});

/**
 * Request OTP
 *
 * Requests a one-time password (OTP) for customer authentication. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const accountsAuthRequestOtpMutation = (options?: Partial<Options<AccountsAuthRequestOtpData>>): UseMutationOptions<AccountsAuthRequestOtpResponse, AccountsAuthRequestOtpError, Options<AccountsAuthRequestOtpData>> => {
    const mutationOptions: UseMutationOptions<AccountsAuthRequestOtpResponse, AccountsAuthRequestOtpError, Options<AccountsAuthRequestOtpData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().accountsAuthRequestOtp({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Verify OTP
 *
 * Verifies the one-time password (OTP) to authenticate the customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const accountsAuthVerifyOtpMutation = (options?: Partial<Options<AccountsAuthVerifyOtpData>>): UseMutationOptions<AccountsAuthVerifyOtpResponse, AccountsAuthVerifyOtpError, Options<AccountsAuthVerifyOtpData>> => {
    const mutationOptions: UseMutationOptions<AccountsAuthVerifyOtpResponse, AccountsAuthVerifyOtpError, Options<AccountsAuthVerifyOtpData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().accountsAuthVerifyOtp({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const accountsAuthGetSessionQueryKey = (options?: Options<AccountsAuthGetSessionData>) => createQueryKey('accountsAuthGetSession', options);

/**
 * Get session
 *
 * Retrieves the current customer session details. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const accountsAuthGetSessionOptions = (options?: Options<AccountsAuthGetSessionData>) => queryOptions<AccountsAuthGetSessionResponse, AccountsAuthGetSessionError, AccountsAuthGetSessionResponse, ReturnType<typeof accountsAuthGetSessionQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().accountsAuthGetSession({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: accountsAuthGetSessionQueryKey(options)
});

/**
 * Sign out
 *
 * Signs out the currently authenticated customer. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const accountsAuthSignOutMutation = (options?: Partial<Options<AccountsAuthSignOutData>>): UseMutationOptions<AccountsAuthSignOutResponse, AccountsAuthSignOutError, Options<AccountsAuthSignOutData>> => {
    const mutationOptions: UseMutationOptions<AccountsAuthSignOutResponse, AccountsAuthSignOutError, Options<AccountsAuthSignOutData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().accountsAuthSignOut({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Initialise checkout
 *
 * Initialises checkout for the current cart and returns the checkout session details. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartCheckoutMutation = (options?: Partial<Options<CartCheckoutData>>): UseMutationOptions<CartCheckoutResponse, CartCheckoutError, Options<CartCheckoutData>> => {
    const mutationOptions: UseMutationOptions<CartCheckoutResponse, CartCheckoutError, Options<CartCheckoutData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().cartCheckout({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const cartCountQueryKey = (options?: Options<CartCountData>) => createQueryKey('cartCount', options);

/**
 * Get cart count
 *
 * Retrieves the current number of items in the cart. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartCountOptions = (options?: Options<CartCountData>) => queryOptions<CartCountResponse, CartCountError, CartCountResponse, ReturnType<typeof cartCountQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().cartCount({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cartCountQueryKey(options)
});

/**
 * Migrate cart
 *
 * Migrates a guest cart into the authenticated customer cart when both are present. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartMigrateMutation = (options?: Partial<Options<CartMigrateData>>): UseMutationOptions<unknown, CartMigrateError, Options<CartMigrateData>> => {
    const mutationOptions: UseMutationOptions<unknown, CartMigrateError, Options<CartMigrateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().cartMigrate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const cartGetQueryKey = (options?: Options<CartGetData>) => createQueryKey('cartGet', options);

/**
 * Get cart
 *
 * Retrieves the current cart contents. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartGetOptions = (options?: Options<CartGetData>) => queryOptions<CartGetResponse, CartGetError, CartGetResponse, ReturnType<typeof cartGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().cartGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cartGetQueryKey(options)
});

/**
 * Add cart item
 *
 * Adds a product or product variant to the current cart. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartAddMutation = (options?: Partial<Options<CartAddData>>): UseMutationOptions<CartAddResponse, CartAddError, Options<CartAddData>> => {
    const mutationOptions: UseMutationOptions<CartAddResponse, CartAddError, Options<CartAddData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().cartAdd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove cart item
 *
 * Removes a cart item from the current cart. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartRemoveMutation = (options?: Partial<Options<CartRemoveData>>): UseMutationOptions<CartRemoveResponse, CartRemoveError, Options<CartRemoveData>> => {
    const mutationOptions: UseMutationOptions<CartRemoveResponse, CartRemoveError, Options<CartRemoveData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().cartRemove({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Update cart item quantity
 *
 * Updates the quantity of an existing cart item. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const cartUpdateMutation = (options?: Partial<Options<CartUpdateData>>): UseMutationOptions<CartUpdateResponse, CartUpdateError, Options<CartUpdateData>> => {
    const mutationOptions: UseMutationOptions<CartUpdateResponse, CartUpdateError, Options<CartUpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().cartUpdate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const collectionsGetCollectionsQueryKey = (options: Options<CollectionsGetCollectionsData>) => createQueryKey('collectionsGetCollections', options);

/**
 * Get all collections
 *
 * Retrieves a list of all collections for the storefront. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const collectionsGetCollectionsOptions = (options?: Options<CollectionsGetCollectionsData>) => queryOptions<CollectionsGetCollectionsResponse, CollectionsGetCollectionsError, CollectionsGetCollectionsResponse, ReturnType<typeof collectionsGetCollectionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().collectionsGetCollections({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: collectionsGetCollectionsQueryKey(options)
});

/**
 * Get collection
 *
 * Retrieves a single collection by the slug provided in the request body. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const collectionsGetCollectionMutation = (options?: Partial<Options<CollectionsGetCollectionData>>): UseMutationOptions<CollectionsGetCollectionResponse, CollectionsGetCollectionError, Options<CollectionsGetCollectionData>> => {
    const mutationOptions: UseMutationOptions<CollectionsGetCollectionResponse, CollectionsGetCollectionError, Options<CollectionsGetCollectionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().collectionsGetCollection({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Get collection products
 *
 * Retrieves products that belong to the collection identified by the slug in the request body. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const collectionsGetCollectionProductsMutation = (options?: Partial<Options<CollectionsGetCollectionProductsData>>): UseMutationOptions<CollectionsGetCollectionProductsResponse, CollectionsGetCollectionProductsError, Options<CollectionsGetCollectionProductsData>> => {
    const mutationOptions: UseMutationOptions<CollectionsGetCollectionProductsResponse, CollectionsGetCollectionProductsError, Options<CollectionsGetCollectionProductsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().collectionsGetCollectionProducts({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const productsGetSearchProductsQueryKey = (options?: Options<ProductsGetSearchProductsData>) => createQueryKey('productsGetSearchProducts', options);

/**
 * Get search products
 *
 * Retrieves a list of products suitable for search. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const productsGetSearchProductsOptions = (options?: Options<ProductsGetSearchProductsData>) => queryOptions<ProductsGetSearchProductsResponse, ProductsGetSearchProductsError, ProductsGetSearchProductsResponse, ReturnType<typeof productsGetSearchProductsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().productsGetSearchProducts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: productsGetSearchProductsQueryKey(options)
});

export const productsGetInfiniteProductsQueryKey = (options: Options<ProductsGetInfiniteProductsData>) => createQueryKey('productsGetInfiniteProducts', options);

/**
 * Get infinite products
 *
 * Retrieves a paginated list of products for a category, collection, or all. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const productsGetInfiniteProductsOptions = (options?: Options<ProductsGetInfiniteProductsData>) => queryOptions<ProductsGetInfiniteProductsResponse, ProductsGetInfiniteProductsError, ProductsGetInfiniteProductsResponse, ReturnType<typeof productsGetInfiniteProductsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().productsGetInfiniteProducts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: productsGetInfiniteProductsQueryKey(options)
});

export const productsGetTrendingProductsQueryKey = (options?: Options<ProductsGetTrendingProductsData>) => createQueryKey('productsGetTrendingProducts', options);

/**
 * Get trending products
 *
 * Retrieves a list of trending products. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const productsGetTrendingProductsOptions = (options?: Options<ProductsGetTrendingProductsData>) => queryOptions<ProductsGetTrendingProductsResponse, ProductsGetTrendingProductsError, ProductsGetTrendingProductsResponse, ReturnType<typeof productsGetTrendingProductsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().productsGetTrendingProducts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: productsGetTrendingProductsQueryKey(options)
});

/**
 * Get product
 *
 * Retrieves a single product by the slug provided in the request body. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const productsGetProductMutation = (options?: Partial<Options<ProductsGetProductData>>): UseMutationOptions<ProductsGetProductResponse, ProductsGetProductError, Options<ProductsGetProductData>> => {
    const mutationOptions: UseMutationOptions<ProductsGetProductResponse, ProductsGetProductError, Options<ProductsGetProductData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().productsGetProduct({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const contentBlocksQueryKey = (options?: Options<ContentBlocksData>) => createQueryKey('contentBlocks', options);

/**
 * Get content blocks
 *
 * Retrieves all configured storefront content blocks for the current store. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const contentBlocksOptions = (options?: Options<ContentBlocksData>) => queryOptions<ContentBlocksResponse, ContentBlocksError, ContentBlocksResponse, ReturnType<typeof contentBlocksQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().contentBlocks({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: contentBlocksQueryKey(options)
});

export const contentBlockQueryKey = (options: Options<ContentBlockData>) => createQueryKey('contentBlock', options);

/**
 * Get content block
 *
 * Retrieves a single storefront content block by its id. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const contentBlockOptions = (options?: Options<ContentBlockData>) => queryOptions<ContentBlockResponse, ContentBlockError, ContentBlockResponse, ReturnType<typeof contentBlockQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().contentBlock({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: contentBlockQueryKey(options)
});

export const contentBannerTextQueryKey = (options?: Options<ContentBannerTextData>) => createQueryKey('contentBannerText', options);

/**
 * Get banner text
 *
 * Retrieves the active banner text for the storefront if enabled. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const contentBannerTextOptions = (options?: Options<ContentBannerTextData>) => queryOptions<ContentBannerTextResponse, ContentBannerTextError, ContentBannerTextResponse, ReturnType<typeof contentBannerTextQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().contentBannerText({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: contentBannerTextQueryKey(options)
});

export const wishlistGetQueryKey = (options?: Options<WishlistGetData>) => createQueryKey('wishlistGet', options);

/**
 * Get wishlist
 *
 * Retrieves the current wishlist contents. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const wishlistGetOptions = (options?: Options<WishlistGetData>) => queryOptions<WishlistGetResponse, WishlistGetError, WishlistGetResponse, ReturnType<typeof wishlistGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await PuddleStorefrontApi.__registry.get().wishlistGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: wishlistGetQueryKey(options)
});

/**
 * Clear wishlist
 *
 * Removes all wishlist items from the current wishlist. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const wishlistClearMutation = (options?: Partial<Options<WishlistClearData>>): UseMutationOptions<WishlistClearResponse, WishlistClearError, Options<WishlistClearData>> => {
    const mutationOptions: UseMutationOptions<WishlistClearResponse, WishlistClearError, Options<WishlistClearData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().wishlistClear({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Add wishlist item
 *
 * Adds a product or product variant to the current wishlist. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const wishlistAddMutation = (options?: Partial<Options<WishlistAddData>>): UseMutationOptions<WishlistAddResponse, WishlistAddError, Options<WishlistAddData>> => {
    const mutationOptions: UseMutationOptions<WishlistAddResponse, WishlistAddError, Options<WishlistAddData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().wishlistAdd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove wishlist item
 *
 * Removes a wishlist item from the current wishlist. Requires `Authorization: Bearer <storefront-public-key>`. Server-side callers must include `x-puddle-storefront-host` when `Origin`/`Referer` is unavailable.
 */
export const wishlistRemoveMutation = (options?: Partial<Options<WishlistRemoveData>>): UseMutationOptions<WishlistRemoveResponse, WishlistRemoveError, Options<WishlistRemoveData>> => {
    const mutationOptions: UseMutationOptions<WishlistRemoveResponse, WishlistRemoveError, Options<WishlistRemoveData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await PuddleStorefrontApi.__registry.get().wishlistRemove({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
